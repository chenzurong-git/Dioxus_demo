# 开发总结：用 Dioxus 在 Windows 上做桌面应用（Open Workbench 移植）

> 时间：2026-08-28 ~ 2026-08-29
> 项目：`Z:\wspace\tina-v821\brandy\up_tool\Dioxus\`（对应 Linux 服务器 `/home/chenzurong/wspace/tina-v821/brandy/up_tool/Dioxus`）
> 本地编译目录：`D:\Desktop_MY\cc_bench\`

## 1. 背景与目标

- 目标一：尝试用 [Dioxus](https://dioxuslabs.com)（Rust 的 React 风格 UI 框架）在 Windows 上写桌面应用。
- 目标二：把 `up_tool\chenji\openworkbench\`（Tauri 2 + React 的复刻版工程，功能还原自 `openworkbench.exe` 的静态分析）移植为 Dioxus 桌面版。
- 当前状态：8 个功能页面全部移植完成，`cargo check --target x86_64-pc-windows-gnu` 0 错误 0 警告，Windows（w64devkit）下可正常编译运行。

## 2. Windows 工具链搭建（无 Visual Studio 方案）

### 路线

装不了 Visual Studio → 不用 MSVC 工具链，走 GNU/MinGW 路线：

- 下载 [w64devkit](https://github.com/skeeto/w64devkit/releases) **x64** 绿色包（自带 gcc / dlltool / mingw32-make），解压到 `D:\w64devkit`。
- 安装 64 位 GNU 工具链并设为默认：

  ```cmd
  rustup toolchain install stable-x86_64-pc-windows-gnu
  rustup default stable-x86_64-pc-windows-gnu
  ```

- `.cargo/config.toml` 配置：`[target.x86_64-pc-windows-gnu] linker = "gcc"`，crates.io 走 USTC 稀疏索引镜像。

### 遇到的问题与解决

| 问题 | 现象 | 解决 |
| --- | --- | --- |
| rustup 引导要求装 VS | “Rust Visual C++ prerequisites” | 不装 VS；直接用 GNU 工具链（w64devkit） |
| 32 位 MinGW（`D:\mingw32`）dlltool 报错 | `error calling dlltool ... 目录名称无效 (os error 267)` | 换 64 位 w64devkit + `stable-x86_64-pc-windows-gnu`；不要把 i686 的 `self-contained` 目录混进 PATH |
| rustup 不在 PATH | `'rustup' 不是内部或外部命令` | 把 rustup 安装目录加入 PATH |
| 网络盘 Z: 上编译失败 | rustc 调 dlltool 报 `os error 267`（与 dlltool 版本无关） | 项目复制到本地盘编译，如 `D:\Desktop_MY\cc_bench` |
| w64devkit 缺 `libgcc_eh` | `ld.exe: cannot find -lgcc_eh` | 把 `libgcc.a` 复制一份命名 `libgcc_eh.a`：<br>`for /r D:\w64devkit %i in (libgcc.a) do @copy /y "%i" "%~dpi\libgcc_eh.a" >nul` |

## 3. WebView2Loader.dll 问题

- 现象：运行 exe 报“找不到 WebView2Loader.dll”。
- 原因：GNU 工具链不会自动把 WebView2 加载器 DLL 放到 exe 旁边（MSVC 会自动带）。
- 解决：新增 `build.rs`，每次构建时自动查找并复制：
  1. 优先从 `webview2-com-sys` 自己的构建输出目录：`target\<triple>\<profile>\build\webview2-com-sys-*\out\<x64|x86>\WebView2Loader.dll`；
  2. 兜底从 cargo registry 源码包（`...\webview2-com-sys-*\x64\WebView2Loader.dll`）找。
- 复制目标：`target\<triple>\<profile>\WebView2Loader.dll`（与 exe 同目录）。
- 注：Win10 用户还需 WebView2 运行时（一般随 Edge 安装；Win11 自带）。

## 4. 移植工作（openworkbench → Dioxus）

### 工程结构

```text
src/
├─ main.rs          # 入口：窗口配置（标题 Open Workbench，1200x760）+ 左侧 8 项导航
├─ store.rs         # 全局状态 Signal<AppStore>；设置/设备/快捷命令持久化到 %APPDATA%\openworkbench\*.json
├─ models.rs        # Device、DeviceInput、QuickCommandWindow、Settings、AdbDevice、HostEntry、FirmwareInfo 等
├─ pages/           # 8 个功能页面
│  ├─ devices.rs    # 我的设备：增删改查、打开对应串口
│  ├─ serial.rs     # 串口终端：波特率/数据位、收发、录制
│  ├─ logs.rs       # 串口日志：列表、读取、删除
│  ├─ firmware.rs   # 固件烧录：上传/删除/烧录（后端占位）
│  ├─ adb.rs        # ADB：扫描/连接、文件浏览、shell 命令、推送
│  ├─ host.rs       # 主机操作：目录浏览、文件读写（受 host_root 约束）
│  ├─ quick.rs      # 快捷命令：多命令窗口、一键发串口
│  └─ settings.rs   # 设置：串口/ADB/主机/烧录/隧道/MCP
└─ services/        # 业务服务层
   ├─ serial.rs     # 串口打开/收发/环形缓冲
   ├─ adb.rs        # adb 封装
   ├─ host.rs       # 主机文件/命令（根目录约束）
   ├─ logs.rs       # 日志落盘与轮转
   ├─ tunnel.rs     # ssh -R 反向隧道子进程
   ├─ mcp.rs        # 轻量 MCP（JSON-RPC over TCP）
   ├─ firmware.rs   # 烧录服务（FlashBackend trait 占位，可替换为 libefex）
   └─ util.rs       # 通用工具
build.rs            # 自动复制 WebView2Loader.dll
```

### 编译错误修复要点（13 个错误 → 0 错误 0 警告）

- Dioxus 0.7 事件回调要求 `FnMut`：`move` 闭包里不能直接移动非 `Copy` 的捕获值（如 `w_send`、`e_full`、`l_name`、`f_name`），改为闭包内 `.clone()`，或拆成两份克隆供不同闭包持有。
- 闭包内直接调 `Signal::set()`（`Write::set(&mut self)`）时，外层绑定必须声明 `mut`；若信号只在 `read()/cloned()/write()` 或按值传给函数时使用，则不需要 `mut`。
- `impl Fn` 回调（如 `field()` 帮助函数）里不能对捕获的信号取 `&mut`：改为按值传 `Signal`（`Signal` 实现了 `Copy`），在函数内部再声明 `mut` 参数。
- 元组构造顺序问题：`(d, d.id, edit)` 会先移动 `d`，先取出 `let d_id = d.id` 再移动。
- 页面组件用大驼峰命名（Dioxus 惯例），加 `#[allow(non_snake_case)]`。
- 移植过来但暂未接线的后端 API（隧道/ADB 部分方法、烧录占位等）加 `#[allow(dead_code)]`。

## 5. 双窗口问题（空控制台窗口）

- 现象：双击 exe 出现两个窗口（应用 + 空控制台），关任意一个都会全部关闭。
- 原因：exe 按“控制台子系统”链接，双击时 Windows 附着一个控制台窗口，控制台被关即终止进程。
- 解决：`src/main.rs` 首行加 `#![windows_subsystem = "windows"]`，改成 GUI 程序。代码中没有 `println!`/控制台输出，无副作用。

## 6. 同步与发布

### 增量同步（本地编译目录）

`sync_cc_bench.bat`：用 robocopy 把源码增量同步到 `D:\Desktop_MY\cc_bench`，
跳过 `target` / `.toolchain` / `.git` / `w64devkit`，本地已编译的 `target` 保留以复用增量编译。
robocopy 退出码 0–7 均为成功，≥8 为失败。

### 正式发布清单

1. 构建 release：

   ```cmd
   cd /d D:\Desktop_MY\cc_bench
   set PATH=D:\w64devkit\bin;%PATH%
   cargo build --release
   ```

   产物：`target\release\dioxus-demo.exe`。改 exe 名可在 `Cargo.toml` 加 `[[bin]] name = "..."`。
2. 发布目录内容：
   - `dioxus-demo.exe`
   - `WebView2Loader.dll`（build.rs 自动复制，确认存在）
   - 可能需要的 w64devkit 运行库 DLL（一般不需要）：用 `objdump -p dioxus-demo.exe | findstr "DLL Name"` 检查，命中 `libgcc_s_seh-1.dll` / `libstdc++-6.dll` / `libwinpthread-1.dll` 则从 `D:\w64devkit\bin` 一并带上。
   - 使用说明：Win10 需 WebView2 运行时；ADB 功能需 `adb.exe`（默认从 PATH 查找，可在设置页指定路径）。
3. 用户数据：首次运行自动生成于 `%APPDATA%\openworkbench\`（settings.json / devices.json / quick_commands.json / logs），无需随包分发。
4. 安装包：CI 已自动产出 **NSIS 安装包**（`packaging/installer.nsi`），安装到
   `Program Files\OpenWorkbench`、创建开始菜单/桌面快捷方式、注册卸载程序，
   可选勾选静默安装 WebView2 运行时；见第 9 节。

## 7. 当前状态与后续建议

- 可编译、可运行（Windows + w64devkit；验证用 `cargo check --target x86_64-pc-windows-gnu`）。
- 已配置 GitHub Actions 自动构建（MSVC release + artifact + tag 发版），正式发布包由 CI 产出（见第 9 节）。
- 烧录后端为 `FlashBackend` 占位实现，真正烧录需接入 libefex（sunxi FES/FEL USB 协议）。
- 隧道/MCP/主机操作的服务层已实现，但部分 API 尚未在 UI 全部接线（以 `#[allow(dead_code)]` 标注）。
- 正式发布前建议：自定义应用图标、更新 `Cargo.toml` 版本号并打 git tag、调好设置页默认值。
- 安全提醒：`host_exec`（主机执行命令）需在设置页显式开启，发布时保持默认关闭。
- 可选后续：Linux 交叉编译需安装 mingw-w64（`sudo apt install gcc-mingw-w64-x86-64`）；CI 自动构建 + GitHub Releases。

## 8. 常用命令速查

```cmd
:: Windows（w64devkit）
cd /d D:\Desktop_MY\cc_bench
set PATH=D:\w64devkit\bin;%PATH%
cargo run                 :: 开发调试
cargo build --release     :: 发布构建

:: 同步代码
Z:\wspace\tina-v821\brandy\up_tool\Dioxus>sync_cc_bench.bat
```

```bash
# Linux（本仓库交叉编译验证）
export PATH=$PWD/.toolchain/cargo/bin:$PATH
export CARGO_HOME=$PWD/.toolchain/cargo
export RUSTUP_HOME=$PWD/.toolchain/rustup
cargo check --target x86_64-pc-windows-gnu
```

## 9. GitHub Actions 自动构建

### 结论

搭本地环境折腾半天（VS 装不了、MinGW/dlltool、网络盘 267、libgcc_eh…），
不如直接交给 GitHub Actions 编译——**正式发布版以后都由 CI 产出，本地不再需要 release 构建**。

### 配置

- 仓库：`https://github.com/chenzurong-git/Dioxus_demo.git`（origin）。
- workflow 文件：`.github/workflows/build.yml`（已随提交 `5c1fa1c` 入库）。
- 运行器：`windows-latest`（自带 VS 构建工具，用 **MSVC** 工具链；产物不依赖 mingw 运行库 DLL，比本地 w64devkit 干净）。
- 触发：push 到 `master`/`main`、PR、手动 `workflow_dispatch`；打 `v*` tag 时自动建 GitHub Release。
- 细节：CI 中删除仓库内 `.cargo/config.toml`（USTC 镜像对 GitHub 运行器不必要，`linker=gcc` 只影响 gnu 目标）；
  `cargo build --release --locked` 按提交的 `Cargo.lock` 构建；`build.rs` 照常复制 WebView2Loader.dll；
  再用 NSIS（`choco install nsis`）打安装包 `dioxus-demo-installer-<版本>.exe`；
  产物（exe + dll + 安装包）统一上传为 `dioxus-demo-windows` artifact，tag 时同时挂到 Release。

### 使用流程

```cmd
:: 日常更新（改完代码后，本地调试用 cc_bench，正式构建走 CI）
Z:\wspace\tina-v821\brandy\up_tool\Dioxus>sync_cc_bench.bat
git add -A && git commit -m "..." && git push

:: 发布新版本
git tag v1.0.0 && git push origin v1.0.0
```

- 构建进度：GitHub 仓库 Actions 页查看；成功后到 artifact 或 Release 下载。
- 首次构建较慢（全量拉依赖，约 5–15 分钟），之后有缓存会快很多。
- 常见失败点：`--locked` 与未提交的 `Cargo.lock` 不一致（改依赖后记得提交 lock）；
  或个别 crate 在 MSVC 下的链接问题（把 Actions 日志贴出来即可定位）。
- 注意：GitHub 沙箱/代理限制下本机无法直接访问 github.com，推送与查看构建在 Windows 侧执行。

### 与本地环境的分工

- 开发调试：`D:\Desktop_MY\cc_bench`（w64devkit，`cargo run`）。
- 正式发布：GitHub Actions 自动构建（MSVC），下载 artifact / Release。
- 本仓库 Linux 侧仅用 `cargo check` 做语法/类型验证（.toolchain 交叉检查）。
